// SPDX-License-Identifier: AGPL-3.0-only
// Copyright (c) 2026 Loaddr Ltd

// TODO: migrate to module system
import { describe, it } from 'vitest';

describe.skip('setupWhatsApp (legacy CLI setup)', () => {
  it('marks whatsapp as configured when wacli is authenticated', () => {});
  it('skips when wacli is not installed and user confirms skip', () => {});
  it('triggers QR auth when wacli is installed but not authenticated', () => {});
});
