// SPDX-License-Identifier: AGPL-3.0-only
// Copyright (c) 2026 Loaddr Ltd

// TODO: migrate to module system
import { describe, it } from 'vitest';

describe.skip('setupGmail (legacy CLI setup)', () => {
  it('marks gmail as configured on successful OAuth', () => {});
  it('warns when npx is not found', () => {});
  it('still configures when OAuth exits non-zero', () => {});
});
