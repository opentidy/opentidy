# OpenTidy — Specification

**Project**: OpenTidy — Open-source personal AI assistant
**Date**: 2026-03-14

Consolidated specification: architecture, technical stack, hooks, and data formats.
Single source of truth for the implementation plan.

---

## 1. Vision

A personal assistant that runs 24/7, capable of managing administrative tasks
autonomously. It works methodically in the background, only disturbs the user when
necessary, and improves over time.

### Design Philosophy

The core insight behind OpenTidy is that Claude Code already has everything needed to
execute complex administrative tasks: skills, MCP servers, browser automation, system
access, and session resume. Building custom orchestration logic — triage engines,
conversation managers, priority queues, knowledge bases — means reimplementing what
Claude does natively, wrapping a brain around a brain.

OpenTidy takes the opposite approach: the backend does plumbing (receive events, launch
Claude, persist state), and Claude decides what to do, how, and in what order. The code
contains zero business logic, zero intelligent routing, zero decision-making. Infrastructure
concerns (deduplication, locks, retry, audit trail, crash recovery) stay in code because
they are infrastructure, not intelligence.

### Target Use Cases

OpenTidy handles multi-step administrative tasks that span days or weeks:

- Invoice tracking and generation
- Document follow-ups with third parties
- Insurance annual reports
- Tax-related correspondence
- Marketplace listings and buyer management
- Expense justification across banking portals

**Common pattern**: multi-step over days/weeks, web navigation, sensitive data (financial,
legal), mandatory human checkpoints at certain moments, unpredictable interactions (MFA,
captcha), long-term follow-up (reminders, verifications).

---

## 2. Core Principles

### Principle 1 — Speed is not a criterion

The assistant handles administrative tasks, not real-time conversations. Whether Claude
takes 10 seconds or 2 minutes to triage an email, the result is the same.

**Consequences**: no latency optimization, no ultra-fine priority queues. Focus on
result quality.

**Exception**: direct interactive use (the user in a Claude Code terminal) — that is
live, but handled natively.

### Principle 2 — Claude Code is the execution engine

All work is done by Claude Code, which uses a Claude Max subscription (flat rate).

**Why**: Claude Code already has the full ecosystem — skills, MCP servers, browser
automation, macOS system access, session resume. Rebuilding this with the API or
the Agent SDK would cost weeks of work for an equivalent result.

### Principle 3 — Budget is not a constraint

No architecture compromises to save tokens or resources. Multiple sessions can run
in parallel. The practical limits of Claude Max (rate limits, parallelism) are
evaluated empirically.

### Principle 4 — Intelligence lives in Claude, not in the code

The backend code contains NO business logic, no triage, no decision-making, no
intelligent routing. It does plumbing: receive events, launch Claude, persist state.
Claude decides what to do, how, and in what order.

**Nuance**: some backend functions are NOT intelligence — event deduplication,
resource locks, retry/backoff, audit trail, crash recovery. That is infrastructure,
not decision-making. It stays in code.

### Principle 5 — No interruption — isolated parallelism

If Claude is working on an invoice, it finishes. An urgent event does not interrupt
it — a new parallel session is launched. Each Claude has its own space and resources.
Resource conflicts are managed by locks.

### Principle 6 — The assistant runs quietly in the background

No second-level reactivity. The assistant works methodically, checks regularly,
advances on tasks.

**Chosen model**: hybrid events + crons.
- Event-driven for external stimuli (Gmail webhook, Telegram message)
- Periodic cron for background work (check tasks, reminders, deadlines)

### Principle 7 — Quick/interactive actions = specialized tool

If a use case demands reactivity or interactivity (real-time conversations, live
bookings), it is a specialized tool/skill — not the core system.

### Principle 8 — Continuous improvement

When Claude cannot accomplish something, it reports the gap in
`workspace/_gaps/gaps.md`. This is a natural improvement backlog, generated by
real usage.

---

## 3. Structural Decision: Context is Finite

### The Reasoning

A single agent with a single context becomes a bottleneck. The real technical
problem is neither capability nor speed — it is that Claude's context window is
finite. You cannot feed it the state of all tasks plus all emails plus all messages.
Even if it fits, quality degrades when context is overloaded (degradation accelerates
after 75% utilization — per Google ADK research).

### The Solution: Focused Sessions per Job

No specialized agents by domain. A single type of agent: a Claude Code instance
loaded with the right context for a specific job.

- Claude works on "invoice tracking" — loaded with: job state, relevant contacts, Gmail access.
- Claude responds to an accountant — loaded with: the email, relevant documents.
- Claude fills an insurance report — loaded with: job state, credentials, browser.

The same Claude, just different contexts. When it finishes, it saves its state and
terminates. Clean session each time.

**What this solves**:
- Finite context — each session loads only what it needs
- Parallelism — multiple independent sessions, no interference
- No drift — short and focused sessions
- No specialization to maintain — one agent type, not N system prompts

---

## 4. Architecture

### Overview

```
┌────────────────────────────────────────────────────────────────────┐
│                          WEB APP                                   │
│                                                                    │
│  Primary user interface:                                           │
│  - Active jobs + status                                             │
│  - Approve/reject actions (previews with context)                  │
│  - Give instructions / create jobs                                 │
│  - Debug (logs, history, sessions)                                 │
│  - MFA/captcha intervention                                        │
│                                                                    │
└────────────────────────┬───────────────────────────────────────────┘
                         │ API
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│                        BACKEND                                     │
│                     (~200-400 lines)                                │
│                                                                    │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐                        │
│  │ RECEIVER │  │ LAUNCHER │  │ STATE MGR │                        │
│  │          │  │          │  │           │                        │
│  │ Webhooks │  │ Launches │  │ Reads/    │                        │
│  │ Crons    │  │ Claude   │  │ writes    │                        │
│  │ Web app  │  │ tmux     │  │ jobs      │                        │
│  └────┬─────┘  └────┬─────┘  └─────┬─────┘                        │
│       │              │              │                               │
│  ┌────┴──────────────┴──────────────┴──────┐                       │
│  │           INFRASTRUCTURE                │                       │
│  │  - Dedup events (content hash)          │                       │
│  │  - Resource locks                       │                       │
│  │  - Retry/backoff                        │                       │
│  │  - Crash recovery                       │                       │
│  └─────────────────────────────────────────┘                       │
│                                                                    │
│  ┌─────────────────────────────────────────┐                       │
│  │         NOTIFICATIONS (Telegram)        │                       │
│  │  Push → user (links to web app)         │                       │
│  └─────────────────────────────────────────┘                       │
│                                                                    │
└────────────────────────┬───────────────────────────────────────────┘
                         │ launches
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│          CLAUDE CODE SESSIONS (autonomous + interactive)            │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ GUARDRAILS (PreToolUse Hooks)                                │  │
│  │                                                              │  │
│  │ Intercept EVERY tool call BEFORE execution.                  │  │
│  │ Claude cannot bypass them.                                   │  │
│  │ Built-in mini-Claude verifier (type: "prompt").              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                    │
│  Session 1: "invoices-2025"                                        │
│    context: workspace/invoices-2025/state.md                       │
│    tools: Gmail MCP, /accounting, browser                          │
│                                                                    │
│  Session 2: "insurance-report"                                     │
│    context: workspace/insurance-report/state.md                    │
│    tools: browser, Bitwarden                                       │
│                                                                    │
│  (N sessions in parallel, isolated)                                │
│                                                                    │
│  Each session:                                                     │
│  1. Reads state.md (current progress)                              │
│  2. Reads the triggering event/instruction                         │
│  3. Works (skills, MCP, browser)                                   │
│     → hooks verify each sensitive action                           │
│  4. Updates state.md                                               │
│  5. If user needed → checkpoint.md + terminates                    │
│  6. If done → status "completed" + terminates                      │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

### Monorepo (pnpm workspaces)

```
opentidy/
├── pnpm-workspace.yaml
├── package.json              # "preinstall": "npx only-allow pnpm"
├── packages/
│   └── shared/               # TypeScript types, Zod schemas
├── apps/
│   ├── backend/              # Hono API, daemon, launcher, receiver
│   └── web/                  # React SPA, Vite
```

No Turborepo for now — pnpm workspaces suffice for 2 apps + 1 package.
If it grows, Turborepo plugs in on top without changing anything.

---

## 5. Components in Detail

### 5.1 RECEIVER — Event Ingestion

Receives everything that can trigger work and transforms it into a uniform event.

**Event Sources**:

| Type | Source | Examples |
|---|---|---|
| External push | Gmail webhook | New email received |
| Polling | SMS/WhatsApp watchers | New message (every 5 min) |
| Cron | Background work | "Check tasks, advance what you can" |
| Cron | Deadlines | "The insurance report is due in 3 days" |
| Cron | Follow-ups | "No response from the vendor in 3 days" |
| User instruction | Web app | "List this desk for sale on the marketplace" |
| User instruction | Claude Code interactive | User works directly |
| User instruction | Telegram | Response to a checkpoint |

**Uniform format**: source, content, timestamp, metadata (sender, etc.).

**Triage / routing**: Claude does the triage (principle #4).
One `claude -p` call per event. The backend passes the **complete content of each
state.md** in the prompt — Claude sees the objective, the journal, and especially
the `## Waiting for` sections (resume criteria), enabling much more precise
matching between an incoming event and the right job.

```
claude -p --system-prompt "Triage mode. Respond in JSON only." \
  "Active jobs (full content of each state.md):\n\n--- invoices-2025 ---\n# Invoices 2025\nSTATUS: IN PROGRESS\n## Waiting for\nEmail sent to billing@vendor.com...\n\n---\n\nEvent:\nEmail from billing@vendor.com: March invoice"
```

Expected JSON response (3 cases):
```json
{ "jobIds": ["invoices-2025"] }
{ "suggestion": { "title": "...", "urgency": "normal", "source": "gmail", "why": "..." } }
{ "ignore": true, "reason": "marketing spam" }
```

Notes:
- `jobIds` is an array — an event can concern multiple jobs
- If the job is already locked, the event is added to the job's CLAUDE.md
  ("Pending events" section) for processing at the next launch/resume
- No batching — one triage per event, dedup eliminates duplicates upstream
- Rate limits: if `claude -p` is rate-limited (429), exponential backoff

**Fundamental rule: Claude never creates a job itself.** Only the user can
create a job (via the web app) or approve a suggestion from Claude.

### 5.2 WORKSPACE — Job State

Each active job has a directory in `workspace/` with markdown files.
No database for state — human-readable files that Claude can also read.

```
workspace/
├── invoices-2025/
│   ├── state.md          # current state, next steps, condensed history
│   ├── checkpoint.md     # if waiting for user: what, why, options
│   └── artifacts/        # produced files (invoice PDFs, etc.)
│
├── insurance-report-2025/
│   ├── state.md
│   └── artifacts/
│
├── _inbox/
│   └── events.md         # events not yet attached to a job
│
├── _suggestions/         # jobs suggested by Claude, pending user approval
│   ├── tax-follow-up.md
│   └── missing-timesheet-june.md
│
├── _gaps/
│   └── gaps.md           # limitations detected by Claude (self-analysis)
│
└── _audit/
    └── actions.log       # history of all external actions
```

#### The state.md File (Heart of the System)

This is Claude's memory for a job. It contains everything Claude needs to
resume work in a new session.

```markdown
# Invoice Tracking 2025-2026

## Objective
Verify that all monthly invoices have been sent.

## Current State
STATUS: IN PROGRESS
Last action: 2026-03-13

## Completed
- Jan 2025: invoice #2025-001 sent on 02/05 ✓
- Feb 2025: invoice #2025-002 sent on 03/03 ✓

## Remaining
- Apr 2025: timesheet found (120h), invoice to create
- May 2025: timesheet MISSING — email sent to vendor on 03/12

## Waiting for
Email sent to billing@vendor.com on 03/12 for the May timesheet.
Follow up if no response before 2026-03-16.

## Contacts
- Vendor billing: billing@vendor.com

## Notes
- Rate: [hourly rate], currency EUR
- Invoice format: use /accounting skill with vendor template
```

**Condensation**: Claude manages the size of state.md itself (principle #4).
If there is too much history, it condenses older entries.

#### The `## Waiting for` Section (Resume Criteria)

Optional section of state.md, free-form text. Claude writes it when it cannot
advance because it is waiting for external information (email response, document,
third-party confirmation). Distinct from `checkpoint.md` which signals a need for
user intervention.

**Role in the system:**
- **Triage**: the prompt receives the full state.md — the `## Waiting for` section
  helps Claude match an incoming event with the right job (e.g., "this job is
  waiting for an email from contact@example.com" + an email arrives from that
  contact → match)
- **Checkup**: the prompt knows that a job with `## Waiting for` should not be
  relaunched unless a follow-up date is mentioned and has passed
- **Launcher**: when a session is relaunched (event or checkup), the backend
  automatically clears the `## Waiting for` section in state.md before launching
  Claude
- **UI**: the first line of the section is displayed on the job card in the web
  app, so the user can see at a glance what a job is waiting for

Claude has one single responsibility: write the section when it leaves. The cleanup
on return is handled by the backend, not by Claude.

#### The checkpoint.md File

When Claude needs the user (question, validation, missing information), it writes
a checkpoint.

```markdown
# Checkpoint — Awaiting Validation

## What I did
Created 2 invoices for April and May 2025.

## What I need from you
Validate the invoices before sending.

## Details
- April invoice: 120h × $100 = $12,000 → artifacts/invoice-2025-04.pdf
- May invoice: 140h × $100 = $14,000 → artifacts/invoice-2025-05.pdf

## Options
1. [Send both] → I send to billing@vendor.com
2. [Modify] → tell me what to change
3. [Cancel] → I cancel and wait for your instructions
```

### 5.3 LAUNCHER — Claude Session Management

Launches Claude Code sessions with the right context. Two execution modes:

**Autonomous mode (default): `claude -p` child process.**

Sessions run as Node.js child processes via `claude -p --output-format stream-json`.
Process exit = reliable end-of-session signal. No polling, no hooks for lifecycle.

Advantages vs tmux:
- Deterministic lifecycle: process exit → `handleAutonomousExit()` — no hooks/polling needed
- NDJSON stdout parsable in real time (stream events for the frontend)
- Automatic post-session agent (memory extraction, gaps, journal)
- Simpler: no tmux capture-pane, no idle detection, no nudging

**Interactive mode ("Take over"): tmux for the user.**

When the user wants to interact directly:
1. Click "Take over" in the web app
2. Backend kills the autonomous child process
3. Launches `claude --resume <session-id>` in tmux
4. User interacts via ttyd (terminal in the web app)
5. "Hand back" kills tmux and relaunches in autonomous mode

**Context loaded via CLAUDE.md (2 levels)**:

Level 1 — `workspace/CLAUDE.md` (global, written once, shared by all sessions):
- Identity (personal assistant, language, writing style)
- How to work (read state.md, update journal, write checkpoint.md if blocked)
- Expected formats (state.md, checkpoint.md, suggestions, gaps)
- Available tools (Gmail MCP, Camoufox, Bitwarden, etc.)
- Security rules (do not retry if a hook refuses)

Level 2 — `workspace/<job>/CLAUDE.md` (generated by backend at each launch):
- Job objective
- Confirm mode (yes/no)
- Triggering event or instruction
- Relevant contacts (pre-fetched from macOS Contacts)

Claude Code automatically loads CLAUDE.md files from the cwd + parents. This persists
across `--resume`. No need to re-pass context via CLI.

**Concrete autonomous command**:
```
# Node.js child process (spawn)
claude -p --output-format stream-json --verbose --dangerously-skip-permissions \
  [--plugin-dir plugins/opentidy-hooks] [--resume <session-id>] "<instruction>"
# cwd = workspace/<job-id>/
```
The instruction/event is passed as a CLI argument. The job's CLAUDE.md provides context.

**Interactive command (tmux, "Take over" only)**:
```
tmux new-session -d -s opentidy-<job-id> \
  "cd workspace/<job-id> && claude --dangerously-skip-permissions --resume <session-id>"
```

**For sweep and triage** (`claude -p`, one-shot, no resume):
```
claude -p --system-prompt "Sweep mode. Analyze workspace/..." "Read workspace/*/state.md..."
claude -p --system-prompt "Triage mode." "Event: email from accountant@firm.com..."
```
No dedicated CLAUDE.md — `--system-prompt` suffices for one-shot calls.

**Parallelism**: multiple child processes at the same time, each on a different job.
Per-job locks (PID, `/tmp/opentidy-locks/`) prevent two sessions from working on the
same job.

**Claude sessions**:
- **`--dangerously-skip-permissions`** on all sessions — disables Claude Code's
  built-in permission prompts. Security is ensured by PreToolUse hooks (guardrails),
  not by the built-in permission system. Hooks fire BEFORE the permission check,
  so they remain active.
- PID lock per job in `/tmp/opentidy-locks/`
- Crash recovery: reconciles tmux survivors (interactive) + relaunches orphaned
  IN PROGRESS jobs (autonomous)
- Session ID persisted in `workspace/<job>/.session-id` for resume

**Browser: Camoufox** (not Chrome/Playwright). Each session has its own Camoufox
instance with an isolated profile. Advantages:
- Full parallelism — no browser lock between agents
- Anti-detection (not flagged as a bot by websites)
- Persistent sessions per profile (cookies, logins preserved between sessions)
- The user keeps Chrome for themselves, zero interference

**State detection**: in autonomous mode, lifecycle is managed by process exit →
`handleAutonomousExit()`. The `type: "command"` hooks remain for audit and
interactive mode.

| Hook | Signal | Backend Action |
|---|---|---|
| `idle_prompt` | Claude awaits input (interactive mode) | Notification + SSE |
| `SessionEnd` | Session ended | Audit log + SSE (lifecycle managed by process exit in autonomous) |
| `Stop` | Claude stops generating | Audit log + SSE |
| `PostToolUse` | Action executed | Audit log |

**Post-session agent**: runs automatically after process exit in
`handleAutonomousExit()` — memory extraction, gap verification, journal. Triggered
only if the transcript is substantial.

**Checkpoint.md**: optional (best-effort). If Claude writes it → structured summary
in the app. In autonomous mode, process exit triggers state verification (COMPLETED,
BLOCKED, checkpoint).

### 5.4 GUARDRAILS — PreToolUse Hooks

This is the most critical component. Claude has access to everything: emails, banking,
invoices, browser. A mistake has real consequences.

#### The Fundamental Problem

The most dangerous case: Claude is CONFIDENT but WRONG. It will not trigger its own
guardrails because it thinks everything is fine.

Key stats:
- If an agent has 85% accuracy per action, a 10-step workflow succeeds only 20% of
  the time (compounding errors)
- CMU 2025: LLMs remain confident even when they are wrong
- RLHF worsens the problem (49.71% accuracy with 39.25% calibration error)

#### The Solution: PreToolUse Hooks `type: "prompt"`

Claude Code has **PreToolUse hooks** — code that runs automatically, on the SYSTEM
side, before every tool call. This is NOT an instruction to Claude. Claude does not
call them, cannot skip them, does not even know they exist.

```
Claude decides: "I'll send this email"
    ↓
Claude calls the tool: gmail.send(...)
    ↓
AUTOMATICALLY, BEFORE execution:
    → the PreToolUse hook fires
    → mini-Claude evaluates the action (separate context, not the same session)
    → decision: ALLOW / DENY / ASK
    ↓
If ALLOW → the action executes
If DENY  → Claude receives "action denied: [reason]"
If ASK   → the user is notified and must approve
```

The `type: "prompt"` hook is a mini-Claude verifier BUILT INTO the hooks system.
Its own separate context. This is NOT the same session verifying itself.

#### Centralized Hook Configuration

**2 types of hooks, 2 distinct roles**:
- `type: "prompt"` → **Guardrails** (mini-Claude evaluates ALLOW/DENY). Blocking.
- `type: "command"` → **Detection + audit** (notifies the backend). Non-blocking.

Both coexist on the same matcher (parallel execution).

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__gmail__send|mcp__gmail__reply|mcp__gmail__draft",
        "hooks": [
          { "type": "prompt", "prompt": "Verify this email send. Rules: never make a payment without approval, verify amount and recipient consistency, flag any anomaly.", "timeout": 30 },
          { "type": "command", "command": "curl -s -X POST http://localhost:3001/api/hooks -d @-" }
        ]
      },
      {
        "matcher": "mcp__camofox__click|mcp__camofox__fill_form|mcp__camofox__camofox_evaluate_js",
        "hooks": [
          { "type": "prompt", "prompt": "Verify this click/form. If it is a payment button, financial submission, or irreversible confirmation, DENY. Otherwise ALLOW.", "timeout": 10 },
          { "type": "command", "command": "curl -s -X POST http://localhost:3001/api/hooks -d @-" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__gmail__|mcp__camofox__",
        "hooks": [
          { "type": "command", "command": "curl -s -X POST http://localhost:3001/api/hooks -d @-" }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "idle_prompt",
        "hooks": [
          { "type": "command", "command": "curl -s -X POST http://localhost:3001/api/hooks -d @-" }
        ]
      }
    ],
    "SessionEnd": [
      { "type": "command", "command": "curl -s -X POST http://localhost:3001/api/hooks -d @-" }
    ]
  }
}
```

#### Hook Coverage

| Tool | Hook? | Timeout | Justification |
|---|---|---|---|
| Gmail send/reply/draft | Yes (prompt) | 30s | Irreversible external actions |
| Camoufox click/fill_form | Yes (prompt) | 10s | Can trigger payments/submissions |
| Camoufox evaluate/run_code | Yes (prompt) | 10s | Arbitrary JS execution |
| Bash (network patterns) | Yes (command) | 10s | curl POST, ssh, scp = external actions |
| Gmail search/read | No | — | Read-only, zero risk |
| Camoufox navigate/snapshot | No | — | Navigation/reading, zero risk |
| Read/Grep/Glob/Write internal | No | — | Local operations, zero risk |

#### The 4 DNA Rules

1. **Every irreversible action → human** (hook ASK)
2. **Every external action → verified before execution** (hook ALLOW/DENY)
3. **Every detected anomaly → reported** (hook + notification)
4. **Everything is logged → repairable after the fact** (PostToolUse audit trail)

#### Honest Limitations

1. The mini-Claude can also be wrong — but two independent Claudes making the same mistake is less likely
2. The browser remains the weakest point — the `element` field helps a lot but "Submit" does not always say what is being submitted
3. Browser slowdown — 10s per significant click. Acceptable (principle #1)
4. Prompt hooks use Claude context — to be monitored
5. Unanticipated cases — no system covers 100%. The ultimate safety net is the audit trail + repairability

### 5.5 WEB APP — Primary User Interface

The web app is the primary interface for the user.

**Main views**:

| View | Route | Content |
|---|---|---|
| Home | `/` | Active jobs + status, pending actions, suggestions, active sessions, recent activity |
| Jobs | `/jobs` | List with Active/Completed/Blocked filters, search |
| Job | `/job/:id` | Rendered state.md, checkpoint summary, sidebar (session, files, history), instruction bar |
| Terminal | `/terminal` | Tmux sessions (interactive mode) — direct interaction with Claude |
| New | `/new` | Create a job + recommendations below |
| Improvements | `/improvements` | Limitations detected by the assistant, improvement backlog |

**Checkpoints**: when Claude writes a checkpoint.md, the web app displays a short
summary (1-2 lines) on the job page and on the Home, with an "Open terminal" button.
The user opens the terminal, reads the detail, and responds directly in the conversation.
No dedicated checkpoint pages, no action buttons in the UI (the terminal is the right
place for that).

**Mobile**: responsive PWA, no native app. Desktop = left icon rail, mobile = bottom tab bar.

### 5.6 NOTIFICATIONS — Telegram (Reduced Role)

Telegram is no longer the primary interface. It serves only as a push notification
channel to the user with a link to the web app.

**Types**:
- "April invoice ready to validate → [View in app]"
- "Blocked on insurance portal (MFA) → [Intervene]"
- "Follow-up sent to vendor for May timesheet"

NO actions from Telegram — just notifications. Actions require the visual context
of the app.

### 5.7 IMPROVEMENTS — Limitation Detection

When Claude cannot accomplish something, it writes to
`workspace/_gaps/gaps.md`:

```markdown
## 2026-03-14 — Insurance portal login
Problem: The site requires MFA via a mobile authenticator app.
Impact: I cannot fill the annual report.
Suggestion: Add a skill to read TOTP codes.
```

Visible on the "Improvements" page of the web app. Logs/audit trail: no dedicated
page, recent activity on the dashboard with a "View full logs" link.

### 5.8 SUGGESTIONS — Claude Proposes, User Decides

Claude cannot create jobs. It creates suggestions in `workspace/_suggestions/`.

**Sources**:
- Incoming event with no matching existing job
- Sweep observation (deadline, follow-up)
- Opportunistic discovery while working on another job

**Format** (`workspace/_suggestions/<slug>.md`):

```markdown
# Suggestion — Tax follow-up

URGENCY: urgent
SOURCE: Email received from tax@authority.gov on 03/12
DATE: 2026-03-14

## Summary
Tax authority email received 2 weeks ago, no response.

## Why
Tax deadline end of March. No existing job for tracking.

## What I would do
Create a job, analyze the email, prepare the requested documents.
```

**Urgency levels**: `urgent` (near deadline), `normal` (handle when possible), `low` (opportunity).

**In the web app**: dedicated section on the Home. Two actions: "Create Job" or "Ignore".
Urgency indicated visually by a colored left border. Urgent ones trigger a Telegram notification.

---

## 6. Main Flows

### Flow 1: External Event → Action

```
1. Gmail webhook → RECEIVER gets "email from accountant@firm.com"
2. RECEIVER dedup → not a duplicate → creates an event
3. LAUNCHER launches Claude (autonomous child process) with the event
4. Claude:
   a. Reads the email (Gmail MCP)
   b. Scans workspace/ jobs → matches "accountant-request"
   c. Reads workspace/accountant-request/state.md
   d. Prepares the response with supporting documents
   e. Calls gmail.reply(...)
      → PreToolUse HOOK fires automatically
      → Mini-Claude verifies: "coherent reply, known recipient → ALLOW"
      OR → "abnormal amount → DENY" OR → "first interaction → ASK"
   f. If ALLOW → email sent, PostToolUse logs the action
   g. If ASK → user notified, must approve in the app
   h. Updates state.md
5. NOTIFICATION → Telegram if relevant
```

### Flow 2: Background Work (Cron)

```
1. Periodic cron → LAUNCHER launches Claude sweep (claude -p)
2. Claude sweep:
   a. Scans workspace/*/state.md
   b. Identifies jobs that need action
   c. Creates suggestions if needed
   d. Returns the list of jobs to launch
3. Backend parses → launches focused autonomous sessions
4. Each session works independently
5. NOTIFICATION → relevant results
```

### Flow 3: User Instruction

```
1. User opens the web app → "List this desk for sale on the marketplace, price 300€"
2. BACKEND creates workspace/marketplace-desk/ + initial state.md
3. LAUNCHER launches Claude with the instruction + state.md
4. Claude:
   a. Reads state.md, starts working
   b. Begins on the marketplace (browser)
   c. Needs photos → checkpoint.md
5. NOTIFICATION → Telegram → link to web app
6. User sends photos via the app
7. LAUNCHER relaunches Claude with the photos
8. Claude creates the listing, preview → checkpoint.md
9. User validates → listing published
```

### Flow 4: MFA/Captcha Block

```
1. Claude works on the insurance report (autonomous session)
2. Goes to insurance portal → login → Bitwarden → OK
3. The site asks for an MFA code
4. Claude writes checkpoint.md + STATUS: BLOCKED
5. Process exit → handleAutonomousExit() → Telegram notification
6. User clicks "Take over" → interactive tmux via ttyd
7. User resolves MFA → "Hand back" → autonomous relaunch
```

---

## 7. Human Intervention and Session Lifecycle

### Autonomous Mode (Default)

In autonomous mode, Claude works as a child process. When it is blocked:

1. It writes checkpoint.md with what it needs
2. It sets `STATUS: BLOCKED` in state.md
3. The process terminates (exit)
4. `handleAutonomousExit()` detects the state → Telegram notification + SSE

**To intervene**: the user clicks "Take over" in the web app. The backend kills the
child process and launches `claude --resume <session-id>` in tmux. The user interacts
via ttyd. When done → "Hand back" → kill tmux → autonomous relaunch.

### Interactive Mode ("Take Over")

When the user is in interactive mode (tmux via ttyd):
- Idle timer available (1h default) — `idle_prompt` hook → notification
- If the user does not respond (timeout): `tmux send-keys` to request a save
- Timer cancellation: next hook from the session → `cancelIdleTimer()`

### Post-Session Agent

After each process exit in autonomous mode, `handleAutonomousExit()`:
1. Reads state.md → determines COMPLETED / BLOCKED / IN PROGRESS
2. Cleanup: release lock, cancel timer, kill ttyd, SSE events
3. If transcript is substantial → launches the memory agent (extraction)
4. Appropriate notifications (completed, checkpoint, etc.)

### Resuming a Session

`claude --resume <session-id>` (stored in `workspace/<job>/.session-id`).
The session_id is captured from the `result` event of the NDJSON stream in
autonomous mode.

### Crash Recovery

At startup, the backend reconciles in two passes:

1. **Tmux sessions (interactive)**: `tmux list-sessions` → filter `opentidy-*` →
   rebuild sessions with mode `interactive`
2. **Orphaned jobs (autonomous)**: scan `workspace/*/state.md` → relaunch those
   that are IN PROGRESS and not waiting
3. Cleanup stale locks via `cleanupStaleLocks()`

---

## 8. Cron Sweep — Periodic `claude -p`

**Decision**: `setInterval` in the backend + `claude -p` for the scan.

```
setInterval(sweep, SWEEP_INTERVAL_MS)  // default: 1h, configurable via env var

sweep():
  claude -p \
    --system-prompt "Checkup mode. Analyze workspace/. If a job has a
    '## Waiting for' section, do NOT relaunch it unless a follow-up date is
    mentioned and has passed. Respond in JSON: { launch: [...], suggestions: [...] }" \
    --allowedTools "Read,Glob,Grep,Write" \
    "Read workspace/*/state.md. For each active job, tell me if an action is
    needed (deadline, follow-up, work to advance).
    Create suggestions in _suggestions/ if needed."
  → backend parses the JSON → launches focused autonomous sessions
```

**`--system-prompt` for one-shots**: the sweep and triage use `claude -p` with
`--system-prompt` instead of CLAUDE.md (no resume, no persistence needed).
`--allowedTools` restricts to reading + Write (to create suggestions).

**Note**: `claude -p` is now also the primary execution mode for job sessions
(with `--output-format stream-json`). The sweep/triage remain distinct one-shots
because they have no CLAUDE.md context nor resume.

`setInterval` suffices for a single timer. No need for `node-cron`.

#### Approaches Explored and Rejected

| Approach | Pro | Con | Verdict |
|---|---|---|---|
| Tmux session for sweep | Consistent with the rest | More indirect, intermediate file | **Rejected** |
| Pure backend with structured metadata | Zero Claude sessions | Fragile — Claude does not always write metadata correctly, optimizes something unnecessary (Claude Max unlimited) | **Rejected** |
| Hybrid (2 separate jobs) | Separation of concerns | Same issues as metadata + added complexity | **Rejected** |

---

## 9. Technical Stack

### 9.1 Infrastructure

| Component | Choice | Reason |
|---|---|---|
| Machine | Dedicated Mac Mini, 24/7 | Native macOS for AppleScript, Contacts, Messages, full system access |
| Backend daemon | macOS LaunchAgent (`com.opentidy.agent.plist`) | Automatic restart, system logs |
| Agent sessions | Agent-agnostic via `AgentAdapter` (Claude stable, Gemini/Copilot experimental) | Child process (autonomous) + tmux (interactive), reliable lifecycle via process exit |
| Browser | Camoufox | Anti-detection, isolated profiles, parallelism |
| Frontend hosting | Coolify | Multi-stage Dockerfile, automatic deploy |
| Network | Cloudflare Tunnel | No open ports, secure access |
| Logs | `~/Library/Logs/` | 5MB rotation |
| Locks | PID in `/tmp/opentidy-locks/` | Crash recovery via dead PID detection |

### 9.2 Setup — Machine Installation

**`opentidy setup`** — interactive modular wizard (9 modules, rerunnable individually):

| Module | What it does |
|--------|-------------|
| User Info | Collects name, email, company, language for CLAUDE.md personalization |
| Telegram | Bot token + chat ID (auto-detect via getUpdates) |
| API Auth | Auto-generates bearer token for web app access |
| Gmail | Gmail MCP OAuth flow (`npx @gongrzhe/server-gmail-autoauth-mcp`) |
| Camoufox | Creates wrapper script for anti-detection browser MCP |
| WhatsApp | Checks wacli install + QR code authentication (optional) |
| Claude Code | Generates `settings.json` dynamically (permissions + mcpServers from configured services), personalizes CLAUDE.md, runs `claude auth login` |
| Cloudflare | Tunnel creation, DNS route, launchd service |
| Permissions | macOS Automation permissions for Messages, Mail, Calendar, etc. |

Key: Claude Code module runs AFTER MCP modules so `generateClaudeSettings()` picks up their config.

**Part 1 — Dependencies** (automated):
- Homebrew, Node.js, pnpm, Claude CLI + OAuth, Camoufox, tmux, cloudflared
- Clone repo, `pnpm install && pnpm build`
- LaunchAgent installation + Cloudflare tunnel

**Part 2 — macOS Permissions** (guided, manual clicks):

The script opens each System Settings panel and waits for confirmation.
The trick: grant permissions to **Terminal.app**. All child processes inherit
automatically.

| Permission | Panel | Grant to |
|---|---|---|
| Full Disk Access | Privacy & Security → Full Disk Access | Terminal.app |
| Accessibility | Privacy & Security → Accessibility | Terminal.app |
| Automation (Messages) | Privacy & Security → Automation | Terminal → Messages |
| Automation (Contacts) | Privacy & Security → Automation | Terminal → Contacts |
| Automation (Calendar) | Privacy & Security → Automation | Terminal → Calendar |
| Automation (Finder) | Privacy & Security → Automation | Terminal → Finder |
| Automation (System Events) | Privacy & Security → Automation | Terminal → System Events |
| Screen Recording | Privacy & Security → Screen Recording | Terminal.app |
| Input Monitoring | Privacy & Security → Input Monitoring | Terminal.app |
| Developer Tools | Privacy & Security → Developer Tools | Terminal.app |

**Rejected approach: PPPC profiles (.mobileconfig)** — requires MDM, impossible
on a personal Mac. Screen Recording and Camera can NEVER be pre-authorized even
with MDM.

### 9.3 Backend

| Component | Choice | Reason |
|---|---|---|
| Runtime | Node.js | TypeScript ecosystem |
| Language | TypeScript strict | SSOT types via packages/shared |
| Web framework | Hono | Built-in SSE, ~7KB, excellent TypeScript |
| Telegram bot | grammY | Mature, retry/rate limiting |
| Validation | Zod | Shared frontend/backend via shared/ |
| Cron sweep | `setInterval` + `claude -p` | See section 8 |
| State (planned) | `better-sqlite3` (`workspace/_data/opentidy.db`) | 4 tables: `claude_processes`, `notifications`, `dedup_hashes`, `sessions` — replaces in-memory state |

**What the backend does (~200-400 lines)**:
1. Receiver — Gmail webhooks, SMS/WhatsApp watchers, web app instructions
2. Launcher — launches autonomous sessions (`claude -p` child process), interactive mode (tmux), manages locks
3. Hook handler — centralized endpoint `/api/hooks`, audit + SSE (lifecycle managed by process exit in autonomous)
4. State manager — reads workspace/ files (state.md, suggestions, gaps)
5. API — routes for the web app (jobs, suggestions, sessions, files)
6. SSE — real-time events to the web app
7. Notifications — Telegram push via grammY
8. Infrastructure — event dedup, locks, retry/backoff, crash recovery, audit trail

**What the backend does NOT do**:
- No AI triage (Claude does it)
- No priority queue
- No business logic
- No conversation manager
- No knowledge base

### 9.4 Frontend

| Component | Choice | Reason |
|---|---|---|
| Framework | React 19 | React Compiler = no useMemo/useCallback |
| Build | Vite | Fast, standard |
| Router | React Router | Well-known, well-documented |
| Styling | Tailwind CSS | Utility-first, no custom CSS |
| State | Zustand | ~1KB, structures live data |
| Terminal | xterm.js | Displays/interacts with tmux sessions (interactive mode via ttyd) |
| Realtime | Native EventSource | SSE from the backend |

**PWA**: responsive (desktop icon rail + mobile tab bar), installable
(manifest, basic service worker).

### 9.5 Testing

| Level | Tool | When | Covers |
|---|---|---|---|
| Unit/integration | Vitest | CI, every push | Backend: receiver, launcher, state manager, infra |
| E2E browser | Playwright | CI, every push | Web app: UI, navigation, interactions |
| System smoke | `/test` skill | End of implementation | Full cross-component flows with real backend |

### 9.6 Tooling

| Component | Choice | Reason |
|---|---|---|
| Package manager | pnpm (enforced) | `"preinstall": "npx only-allow pnpm"` + `"packageManager": "pnpm@10.x"` |
| Linting | ESLint + Prettier | Well-known to Claude |
| TypeScript | strict mode | Type safety |

### 9.7 Shared Packages (`packages/shared/`)

Types and schemas shared between backend and frontend:
- Job types (status, metadata)
- Suggestion types (urgency, source)
- SSE event types
- Zod schemas for API validation
- Checkpoint types

---

## 9.5 Agent Abstraction

OpenTidy is agent-agnostic. Users choose their AI agent at setup time.

### Supported Agents

| Agent | Status | Binary | Notes |
|---|---|---|---|
| Claude Code | **Stable** | `claude` | All features verified. Default. |
| Gemini CLI | Experimental | `gemini` | Candidate — requires upstream verification |
| Copilot CLI | Experimental | `copilot` | Candidate — requires upstream verification |

### Parity Requirements

An agent must support ALL of: headless mode, structured output, session resume, system prompt injection, pre-tool hooks (block/allow), MCP servers, config isolation via env var, permission bypass.

### Architecture

- **Interface:** `AgentAdapter` in `packages/shared/src/types.ts` — `buildArgs()`, `getEnv()`, `readSessionId()`, `writeConfig()`
- **Registry:** `resolveAgent()` in `src/shared/agents/registry.ts` — resolution: `OPENTIDY_AGENT` env → `--agent` flag → `config.json` → fallback `claude`
- **Spawner:** `spawn-agent.ts` replaces `spawn-claude.ts` — uses `adapter.binary` and `adapter.getEnv()`
- **Guardrails:** `plugins/opentidy-hooks/guardrails.json` (unified) → `adapter.writeConfig()` generates native hooks
- **Instructions:** `INSTRUCTIONS.md` (source of truth) → adapter copies to native filename (`CLAUDE.md`, `GEMINI.md`, `AGENTS.md`)

### Adding a New Agent

1. Verify all 8 parity requirements against the agent's documentation
2. Create `src/shared/agents/<name>.ts` implementing `AgentAdapter`
3. Add the agent to `resolveAgent()` in `registry.ts`
4. Run the verification checklist (smoke test: triage + session + interactive)
5. Update `guardrails.json` translation in `writeConfig()` if hooks format differs

---

## 10. Hooks — Technical Reference

### What the Hook Receives (stdin JSON)

```json
{
  "session_id": "abc123",
  "transcript_path": "/path/to/transcript.jsonl",
  "cwd": "/current/working/dir",
  "permission_mode": "ask",
  "hook_event_name": "PreToolUse",
  "tool_name": "mcp__gmail__send",
  "tool_use_id": "toolu_xxx",
  "tool_input": {
    "to": "billing@vendor.com",
    "subject": "April 2025 invoice",
    "body": "Please find attached..."
  }
}
```

Key points:
- `tool_input` contains the COMPLETE parameters of the tool
- `transcript_path` points to the COMPLETE conversation history
- For browser tools, `tool_input` contains an `element` field (textual description)

### What the Hook Can Respond

**Simple (exit codes)**:
- `exit 0` → ALLOW (stdout displayed in the transcript)
- `exit 2` → DENY (stderr sent to Claude as an explanation)
- `exit 1` → non-blocking error

**Structured (JSON stdout)**:
```json
{
  "hookSpecificOutput": {
    "permissionDecision": "allow|deny|ask",
    "updatedInput": {"modified": "params"}
  },
  "systemMessage": "Message injected into Claude's context",
  "additionalContext": "Additional context"
}
```

- `updatedInput` → can MODIFY the tool parameters before execution

### Matchers (Tool Selection)

Support regex, case-sensitive:
```
"Bash"                                    → Bash only
"mcp__gmail__send|mcp__gmail__reply"      → Gmail sends only
"mcp__camofox__click|mcp__camofox__fill_form" → clicks and forms
"*"                                       → everything
```

### Playwright/Camoufox Tools and Risks

| Tool | Key Parameters | Risk |
|---|---|---|
| `browser_navigate` | `url` | Low |
| `browser_click` | `ref`, `element` (description) | Variable |
| `browser_type` | `ref`, `text`, `submit` | Medium |
| `browser_fill_form` | `fields[]` | Medium-high |
| `browser_evaluate` | `function` (JS code) | High |
| `browser_snapshot` | `filename` | None |

### Constraints

- Timeout: 60s default, 600s max
- Hooks loaded at startup: changes require restart
- Parallel hooks if multiple on the same matcher
- No recursion: `type: "prompt"` is native and immune

---

## 11. Approaches Explored and Rejected

| Approach | Idea | What we keep | Why rejected |
|---|---|---|---|
| A — Coded workflows | Predefined steps with checkpoints | Checkpoint concept | Coded steps = rigid. Claude adapts dynamically. |
| B — YAML rules | Routing by pattern matching | — | Breaks as soon as reality is nuanced. |
| C — Single agent | One Claude, one virtual desk | Virtual desk as files | Single context = bottleneck. No parallelism. |
| D — Multi-agent by domain | Accounting/admin/social agents | Isolated parallelism | Claude is already a generalist. Granularity = job, not domain. |
| E — Skills only | Everything in skills, 50-line backend | Intelligence in prompts | We need plumbing. "50 lines" is unrealistic. |
| F — Pure stateless | Each event = fresh session | Fresh sessions = robust | Cold-start overhead. Lost browser state. |
| G — Hybrid | Stateless by default, stateful if needed | Pragmatic compromise | Integrated into final architecture (autonomous = child process, interactive = tmux). |
| H — Custom CLIs | Replace MCP with CLIs | — | "Programming what Claude already knows how to do." |

**Other rejected decisions**:
- **Claude API / Agent SDK**: too expensive, no existing ecosystem
- **Specialized agents by domain**: complexity without value
- **`--allowedTools` for security**: Claude bypasses via browser/bash
- **File watching (chokidar/fs.watch/polling)**: simplified watchdog to fs.watch for `job:updated` SSE only
- **Biome**: Claude knows ESLint+Prettier better
- **Wouter**: too obscure vs React Router
- **Turborepo**: overkill for 2 apps + 1 package
- **npm**: pnpm necessary for workspaces
- **PPPC mobileconfig**: requires MDM
- **Structured metadata in state.md**: fragile, optimizes the unnecessary

---

## 12. API Routes (Derived from Components)

| Method | Route | Description |
|---|---|---|
| POST | `/api/webhook/gmail` | Incoming Gmail webhook |
| POST | `/api/hooks` | Centralized Claude Code hooks endpoint |
| POST | `/api/sweep` | Manually trigger a sweep |
| POST | `/api/job` | Create a job (user instruction) |
| POST | `/api/job/:id/resume` | Relaunch a session with --resume |
| POST | `/api/job/:id/instruction` | Send an instruction to a job |
| POST | `/api/job/:id/upload` | Upload files to artifacts/ |
| POST | `/api/suggestion/:slug/approve` | Approve a suggestion → creates a job |
| POST | `/api/suggestion/:slug/ignore` | Ignore a suggestion |
| POST | `/api/session/:id/timeout` | Simulate a timeout (dev/test) |
| POST | `/api/improvement/:id/resolve` | Mark a gap as resolved |
| GET | `/api/jobs` | List of jobs |
| GET | `/api/job/:id` | Job detail (state.md, checkpoint, artifacts) |
| GET | `/api/suggestions` | List of suggestions |
| GET | `/api/improvements` | List of gaps |
| GET | `/api/sessions` | Active sessions (autonomous + interactive) |
| GET | `/api/notifications/recent` | Recent notifications |
| GET | `/api/events` | SSE — real-time events |

---

## 13. Constraints and Concerns

| Topic | Concern | Solution |
|---|---|---|
| Incomplete plan | Past implementation plans have missed features | Plan driven by comprehensive E2E tests. Each step = test IDs. |
| Claude Max rate limits | Number of parallel sessions unknown | Test empirically. Backend manages backoff. |
| tmux + ttyd + xterm.js | Terminal in the browser (interactive mode) is non-trivial | Isolated component. ttyd exposes tmux via WebSocket → xterm.js. |
| Tunnel security | Backend exposed via Cloudflare | Auth on the API (token or Cloudflare Access). |

---

## 14. External Research — Key Concepts Retained

| Concept | Source | What we retain |
|---|---|---|
| Natural language constitution | IronCurtain | Simple rules → emergent behaviors. Our 4 DNA rules. |
| Reversible Autonomy | Rubrik, IBM STRATUS | Accept errors + make them repairable. Audit trail = safety net. |
| Zero-Trust Agent | Microsoft | Every action checked, limited, logged. |
| Excessive Agency | OWASP LLM06:2025 | 3 root causes: too much functionality, permissions, autonomy. |
| Confident but wrong | CMU 2025 | RLHF makes LLMs confident even when wrong → EXTERNAL verification. |
| Two-level memory | Letta/MemGPT | Core (state.md = RAM) + Archival (history = storage). |
| Degrading context | Google ADK | Degradation accelerates after 75%. → Short and focused sessions. |
| Scaffolding > Raw intelligence | PAI (Miessler) | "Haiku can outperform Opus when the scaffolding is good." |

---

## 15. Future Capabilities (Out of Scope)

The architecture must be flexible enough to plug in these features, not designed for them.

- Phone calls
- Reservations (restaurant, appointments)
- Shopping / online purchases
- Travel management
- Information monitoring
- Continuous recording devices — to explore
